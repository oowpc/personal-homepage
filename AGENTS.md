# AGENTS.md

## 项目目标

本项目用于“算法基础 第一次大作业”，目标是使用 AI Agent 辅助搭建一个可访问的个人主页，并在主页中提供第一篇技术博客入口。

项目应满足以下要求：

- 主页能够正常访问。
- 主页包含个人简介、学习方向、项目或学习记录。
- 主页提供博客入口，并能跳转到第一篇技术博客。
- 博客内容体现个人理解与思考。
- 页面在常见浏览器中显示正常，并具备基本响应式布局能力。

## 技术方案

本项目采用静态网页方案：

- HTML：页面结构。
- CSS：视觉样式与响应式布局。
- GitHub Pages：静态网页部署。

当前不引入前端框架、构建工具或后端服务，便于部署、检查和提交。

## 文件结构

```text
.
├── index.html
├── blog.html
├── styles.css
├── README.md
├── AGENTS.md
└── assets/
    └── hero-study.png
```

文件说明：

- `index.html`：个人主页，包含简介、方向、学习记录和博客入口。
- `blog.html`：第一篇技术博客页面。
- `styles.css`：全站样式、排版和响应式适配。
- `assets/`：图片等静态资源。
- `README.md`：本地预览和 GitHub Pages 部署说明。
- `AGENTS.md`：项目规范与 AI Agent 协作说明。

## 内容规范

1. 个人信息应真实、准确、规范。
2. 博客内容可以由 AI 辅助润色，但核心思路应由本人确认和修改。
3. 页面文字应保持清晰、简洁，避免堆砌无关内容。
4. 所有链接在提交前必须手动检查。
5. 图片资源应放在 `assets/` 目录中，并使用相对路径引用。

## 代码规范

1. 使用语义化 HTML 标签，例如 `header`、`main`、`section`、`article`、`footer`。
2. CSS 类名应表达组件或区域含义，例如 `site-header`、`hero`、`feature-card`。
3. 不使用内联样式，样式统一写在 `styles.css`。
4. 保持文件结构简单，避免为了静态作业引入不必要依赖。
5. 新增页面时应复用统一的页头、页脚和样式。
6. 保证移动端宽度下文本不溢出，按钮和导航可正常使用。

## AI Agent 使用约定

AI Agent 可以协助完成：

- 拆解课程要求。
- 设计页面结构和文件组织。
- 编写 HTML、CSS 和博客初稿。
- 检查链接、资源路径和响应式布局。
- 整理 README、项目规范文档和报告素材。

本人需要完成或确认：

- 替换真实个人信息。
- 审核博客观点是否符合个人理解。
- 检查页面在浏览器中的实际显示效果。
- 完成 GitHub Pages 部署。
- 在作业报告中说明 AI 辅助内容和个人修改内容。

## 开发流程

建议按以下步骤推进：

1. 阅读作业要求，明确必须提交的主页网址和 PDF 报告。
2. 搭建静态页面基本结构。
3. 补充个人简介、学习方向、项目记录和博客内容。
4. 本地打开 `index.html` 检查页面显示和跳转。
5. 上传到 GitHub 仓库并启用 GitHub Pages。
6. 使用线上链接再次检查主页和博客。
7. 截图并撰写作业报告。

## 验收清单

提交前需要确认：

- `index.html` 可以正常打开。
- 主页中存在清晰的博客入口。
- `blog.html` 可以从主页跳转访问。
- 页面样式加载正常。
- `assets/hero-study.png` 图片加载正常。
- 手机或窄屏下页面没有明显错位。
- GitHub Pages 线上链接可以访问。
- 作业报告中包含主页链接、博客入口、实现过程、AI 使用说明和截图。
